import { Department } from './types'

// The department list lived only in the (now deleted) Strapi CMS. Add entries here
// and set `departmentSlug` on the matching jobs in `jobs.ts`.
export const departments: Department[] = []
